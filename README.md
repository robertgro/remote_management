# remote_management

simplifying remote access management on win hosts by wrapping up ssh in a powershell cmd app. Additionally providing switches to configure predefined session params.

# requirements
* to avoid entering a password during the ssh session connecting setup, make sure that ssh public client keys have been exchanged/trusted/stored properly beforehand 
